server {
    listen 80;
    listen [::]:80;

    root /var/www/ai-ticulate-client;
    index index.html;

    server_name localhost:8080;

    location / {
        try_files $uri $uri/ /index.html =404;
    }
}
